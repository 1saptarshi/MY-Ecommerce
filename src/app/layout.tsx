import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";
import { Toaster } from "@/components/ui/toaster";
import { AuthProvider } from "@/contexts/auth-context";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "ShopHub - Modern E-Commerce Platform",
  description: "A comprehensive e-commerce platform with product catalog, shopping cart, and secure payment integration.",
  keywords: ["e-commerce", "shopping", "products", "payment", "Next.js", "TypeScript"],
  authors: [{ name: "ShopHub Team" }],
  icons: {
    icon: "/favicon.ico",
  },
  openGraph: {
    title: "ShopHub - Modern E-Commerce",
    description: "Shop your favorite products with secure payment integration",
    type: "website",
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en" suppressHydrationWarning>
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased bg-background text-foreground`}
      >
        <AuthProvider>
          {children}
          <Toaster />
        </AuthProvider>
      </body>
    </html>
  );
}
